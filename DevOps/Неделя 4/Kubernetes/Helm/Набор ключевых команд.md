---
sr-due: 2026-04-12
sr-interval: 94
sr-ease: 230
---

#sr-due 
```bash
# работа с репозиториями
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm search repo nginx

# установка/обновление/диагностика
helm install web bitnami/nginx --namespace web --create-namespace
helm upgrade web bitnami/nginx -f values.yaml --atomic --timeout 5m
helm status web
helm history web
helm rollback web 1

# инспекция рендеринга и состояния
helm template web ./chart -f values.yaml > out.yaml
helm get values web -a
helm get manifest web

# работа с OCI
helm package ./chart
helm push chart-0.1.0.tgz oci://ghcr.io/<org>/charts
helm pull  oci://ghcr.io/<org>/charts/chart --version 0.1.0
```
[[Набор ключевых команд]]
[[Helm]]