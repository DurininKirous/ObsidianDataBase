---
sr-due: 2025-10-31
sr-interval: 2
sr-ease: 230
---

#sr-due 
```scss
Helm CLI ──(kubeconfig/RBAC)──▶ API Server ──▶ etcd
     │                               │
     ├── рендер шаблонов              ├─ хранение объектов (Deployment, Service…)
     └── хранение истории релиза ◀───┘ (Secrets sh.helm.release.v1.*)
```
- Helm рендерит **локально**, в API улетают уже обычные YAML.
- История релиза хранится **в кластере** (Secrets), а не в твоей машине.
[[Архитектура взаимодействий]]
[[Helm]]