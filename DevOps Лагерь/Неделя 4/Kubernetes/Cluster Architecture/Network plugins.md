Компоненты которые реализуют container network interface (cni).
Она отвечает за предоставление IP-адресов подам и реализацию возможности коммуникации между ними внутри кластера.
[[Network plugins]]
[[Архитектура кластера]]