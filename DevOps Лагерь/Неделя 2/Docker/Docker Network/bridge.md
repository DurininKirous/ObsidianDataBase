---
sr-due: 2025-08-23
sr-interval: 26
sr-ease: 270
---

#sr-due 

- Создаётся по умолчанию: `docker0` интерфейс
- Все контейнеры получают IP из подсети, напр. `172.17.0.0/16`
- Есть NAT: контейнеры могут ходить наружу, но внешний мир не может внутрь без `-p`
- DNS-имена контейнеров недоступны вне `bridge`
[[bridge]]
[[Типы сетей в Docker]]