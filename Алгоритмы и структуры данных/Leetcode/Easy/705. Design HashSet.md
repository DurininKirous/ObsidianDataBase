---
sr-due: 2026-01-28
sr-interval: 55
sr-ease: 210
---

#sr-due 
```python
class MyHashSet:
    def __init__(self):
        self.size = 1000
        self.buckets = [[] for _ in range(self.size)]
    
    def _hash(self, key):
        return key % self.size
    
    def add(self, key):
        h = self._hash(key)
        if key not in self.buckets[h]:
            self.buckets[h].append(key)
    
    def remove(self, key):
        h = self._hash(key)
        if key in self.buckets[h]:
            self.buckets[h].remove(key)
    
    def contains(self, key):
        h = self._hash(key)
        return key in self.buckets[h]
```
### 💡 Идея
Создаём собственную хэш-таблицу с «цепочками» (bucket-list).
- Каждый ключ попадает в корзину по хэш-функции `key % size`.
- В корзине (`list`) хранятся все элементы с одинаковым хэшом.
- Операции `add`, `remove`, `contains` — выполняются за **O(1)** в среднем (линейно только внутри корзины).
[[705. Design HashSet]]
[[Easy]]