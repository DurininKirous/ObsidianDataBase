---
created: 2026-03-15 12:20
tags:
  - status/todo
  - difficulty/medium
  - pattern/two-pointers
  - company/yandex
  - sr-due
leetcode_number: "290"
difficulty: easy
topics: []
submission_date:
sr-due: 2026-04-02
sr-interval: 10
sr-ease: 230
---

# 💡 The What
*О чем задача? Что нужно сделать?*
Given a `pattern` and a string `s`, find if `s` follows the same pattern.

Here **follow** means a full match, such that there is a bijection between a letter in `pattern` and a **non-empty** word in `s`. Specifically:

- Each letter in `pattern` maps to **exactly** one unique word in `s`.
- Each unique word in `s` maps to **exactly** one letter in `pattern`.
- No two letters map to the same word, and no two words map to the same letter.


---

## 🔧 Approach & Solution
*Какой подход? Какая идея решения?*
- Используем два множества: один отображение из pattern в s, второй из s в pattern, проверяем соответствие символов

**Time Complexity:** O(n)
**Space Complexity:** O(n)

---

## 💻 Code

```python
class Solution:
    def isIsomorphic(self, s: str, t: str) -> bool:
        symbols1 = {}
        symbols2 = {}
        if len(s) != len(t):
            return False
        for i in range(len(s)):
            if s[i] not in symbols1:
                symbols1[s[i]] = t[i]
            if t[i] not in symbols2:
                symbols2[t[i]] = s[i]
            if t[i] != symbols1[s[i]] or s[i] != symbols2[t[i]]:
                return False
        return True
```


---

## 🔗 Connections
- **Pattern:** ...
- **Similar Problems:** ...
