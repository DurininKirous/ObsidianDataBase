---
sr-due: 2026-09-09
sr-interval: 170
sr-ease: 230
---

#sr-due 
```python
class Solution:
	def busyStudent(self, startTime: List[int], endTime: List[int], queryTime: int) -> int:
	k = 0
	for i in range(len(startTime)):
		if startTime[i] <= queryTime <= endTime[i]:
			k += 1
return k
```
**Идея**: Проверяем для каждого студента `start <= query <= end`. `sum()` считает True как 1. O(n) time, O(1) space.
[[1450. Number of Students Doing Homework]]
[[Easy]]