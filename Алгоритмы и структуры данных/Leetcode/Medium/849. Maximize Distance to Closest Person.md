---
created: 2026-01-09 10:58
tags:
  - status/todo
  - difficulty/medium
  - company/yandex
  - sr-due
leetcode_number: "849"
difficulty: medium
topics: []
submission_date:
sr-due: 2026-03-29
sr-interval: 79
sr-ease: 230
---

# 💡 The What
*О чем задача? Что нужно сделать?*
You are given an array representing a row of `seats` where `seats[i] = 1` represents a person sitting in the `ith` seat, and `seats[i] = 0` represents that the `ith` seat is empty **(0-indexed)**.

There is at least one empty seat, and at least one person sitting.

Alex wants to sit in the seat such that the distance between him and the closest person to him is maximized. 

Return _that maximum distance to the closest person_.

---

## 🔧 Approach & Solution
*Какой подход? Какая идея решения?*
В массиве есть три вида «пустот» из нулей:
    1. **Префикс** до первой `1` → расстояние = длина префикса.
    2. **Между двумя `1`** → садимся посередине → расстояние = половина промежутка (целая часть).
    3. **Суффикс** после последней `1` → расстояние = длина суффикса.
Один раз проходимся и учитываем все такие ситуации

**Time Complexity:** O(n)
**Space Complexity:** O(1)

### Algorithm:
1. Один линейный проход: держим индекс последней `1` (`prev`), на каждой новой `1` обновляем ответ для случая (2); отдельно учитываем (1) и (3). 
---

## 💻 Code

```python
n = len(seats)
prev = -1
ans = 0
for i, x in enumerate(seats):
	if x == 1:
		if prev == -1:
			ans = max(ans, i)
		else:
			ans = max(ans, (i - prev) // 2)
		prev = i
if prev != -1:
	ans = max(ans, n - 1 - prev)    
return ans
```

---
## ⚔️ VS / Trade-offs

_Альтернативные подходы. Плюсы/Минусы._
- **VS Brute Force:** O(n^2), неэффективно

---

## 🔗 Connections
- **Pattern:** ...
- **Similar Problems:** 
