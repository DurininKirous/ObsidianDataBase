---
created: 2026-01-05 21:15
tags:
  - status/done
  - difficulty/medium
  - pattern/stack
  - sr-due
leetcode_number: "735"
difficulty: medium
topics:
  - stack
submission_date:
sr-due: 2026-04-15
sr-interval: 99
sr-ease: 230
---

# 💡 The What
*О чем задача? Что нужно сделать?*
We are given an array `asteroids` of integers representing asteroids in a row. The indices of the asteroid in the array represent their relative position in space.

For each asteroid, the absolute value represents its size, and the sign represents its direction (positive meaning right, negative meaning left). Each asteroid moves at the same speed.

Find out the state of the asteroids after all collisions. If two asteroids meet, the smaller one will explode. If both are the same size, both will explode. Two asteroids moving in the same direction will never meet.


---

## 🔧 Approach & Solution
*Какой подход? Какая идея решения?*
- Используем стек для моделирования состояния ряда

**Time Complexity:** O(n)
**Space Complexity:** O(n)

### Key Insights:
- 
### Algorithm:
Астероиды добавляются в стек всегда, кроме одного случая конфликта:
    Конфликт: Текущий астероид летит влево (< 0), а на вершине стека — астероид, летящий вправо (> 0).
    Разрешение конфликта (в цикле):
        Если новый больше (abs(new) > top): уничтожаем верхний (pop) и продолжаем проверку следующего в стеке.
        Если равны (abs(new) == top): уничтожаем верхний (pop) и сам новый астероид тоже разрушается.
        Если новый меньше (abs(new) < top): новый астероид разрушается, стек не меняем.
    Если конфликт не возник (или астероид "победил" всех в стеке), добавляем его в стек
---

## 💻 Code

```python
class Solution:
    def asteroidCollision(self, asteroids: List[int]) -> List[int]:
        stack = []
        for asteroid in asteroids:
            alive = True
            while alive and asteroid < 0 and stack and stack[-1] > 0:
                if stack[-1] < abs(asteroid):
                    stack.pop()
                    continue
                elif stack[-1] == abs(asteroid):
                    stack.pop()
                alive = False
            if alive:
                stack.append(asteroid)
        return stack
```

---
## ⚔️ VS / Trade-offs

_Альтернативные подходы. Плюсы/Минусы._
- **VS Brute Force:**
- **Trade-off:**

---

## 🔗 Connections
- **Pattern:** ...
- **Similar Problems:** ...
- **Prerequisites:** ...
