---
sr-due: 2025-12-11
sr-interval: 18
sr-ease: 230
---

#sr-due 
```python
class ZigZagIterator:
    def __init__(self, v1, v2):
        i = 0
        j = 0
        k = 0
        array = []
        while i < len(v1) or j < len(v2):
            if k == 0:
                if i < len(v1):
                    array.append(v1[i])
                    i += 1
                k = 1
            if k == 1:
                if j < len(v2):
                    array.append(v2[j])
                    j += 1
                k = 0
        self.array = array
        self.i = 0

    def hasNext(self):
        if self.i < len(self.array):
            return True
        return False

    def next(self):
        self.i += 1
        return self.array[self.i - 1]
```
## Идея
- Чередуем элементы из двух списков `v1` и `v2`: по очереди берём из каждого.
- Для простоты **предсобираем** общий массив `array`, затем отдаём элементы через `next()`/`hasNext()`.
- Работает корректно, LeetCode принимает.

**Сложность:**
- Время: `O(n + m)`
- Память: `O(n + m)`

**Заметки:** вызывать `next()` только после `hasNext()`.
[[281. Zigzag Iterator]]
[[Medium]]