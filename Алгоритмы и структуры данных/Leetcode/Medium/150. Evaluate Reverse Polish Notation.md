---
sr-due: 2026-07-10
sr-interval: 124
sr-ease: 210
---

#sr-due 
```python
class Solution:
    def evalRPN(self, tokens: List[str]) -> int:
        stack = []
        curr = 0
        for i in tokens:
            if i not in "/*-+":
                stack.append(int(i))
            else:
                a = stack.pop()
                b = stack.pop()
                if i == "+":
                    stack.append(b+a)
                elif i == "-":
                    stack.append(b-a)
                elif i == "*":
                    stack.append(b*a)
                elif i == "/":
                    stack.append(int(b/a))
        return stack[0]
```
**Идея:**  
Используем стек. Проходим по `tokens` слева направо:

- если токен — число, кладём его в стек;
    
- если оператор — достаём два числа `b` и `a` (в таком порядке!), считаем `a op b` и кладём результат обратно в стек.  
    В конце в стеке одно число — это ответ. Деление через `int(b/a)` даёт усечение к нулю.
[[150. Evaluate Reverse Polish Notation]]
[[Medium]]