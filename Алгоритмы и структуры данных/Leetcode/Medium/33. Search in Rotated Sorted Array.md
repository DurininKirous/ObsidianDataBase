---
sr-due: 2026-01-07
sr-interval: 45
sr-ease: 190
---

#sr-due 
func search(nums []int, target int) int {
    left, right := 0, len(nums)-1

    for left <= right {
        mid := left + (right-left)/2
        if nums[mid] == target {
            return mid
        }
        if nums[left] <= nums[mid] { // левая половина отсортирована
            if target >= nums[left] && target < nums[mid] {
                right = mid - 1
            } else {
                left = mid + 1
            }
        } else { // правая половина отсортирована
            if target > nums[mid] && target <= nums[right] {
                left = mid + 1
            } else {
                right = mid - 1
            }
        }
    }
    return -1
}

## Алгоритм
1. Используем бинарный поиск.
2. На каждом шаге:
    - если `nums[mid] == target` → вернуть mid.
    - если `nums[left] <= nums[mid]` → **левая половина отсортирована**:
        - если `target ∈ [nums[left]..nums[mid])` → идём влево (`right = mid-1`),
        - иначе → вправо (`left = mid+1`).
    - иначе → **правая половина отсортирована**:
        - если `target ∈ (nums[mid]..nums[right]]` → идём вправо (`left = mid+1`),
        - иначе → влево (`right = mid-1`).
[[33. Search in Rotated Sorted Array]]
[[Medium]]